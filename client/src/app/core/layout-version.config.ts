/**
 * Default layout version when no `v` query param is present.
 * Use v=1, v=2, v=3 etc. in the URL to override.
 * Change this value to make a different layout the default for landing and demo pages.
 */
export const DEFAULT_LAYOUT_VERSION = 2;
