{
  "name": "fais",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "mongo:status": "node scripts/check-mongo.mjs",
    "setup:dev": "node scripts/setup-dev.mjs",
    "db:reset:clean": "npm --prefix server run migrate:reset:clean -- --drop",
    "dev": "npm run setup:dev && npm run mongo:status && concurrently -n SERVER,CLIENT -c blue,green \"npm --prefix server run dev\" \"npm --prefix client start\"",
    "dev:nocheck": "npm run setup:dev && concurrently -n SERVER,CLIENT -c blue,green \"npm --prefix server run dev\" \"npm --prefix client start\"",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "server:dev": "npm --prefix server run dev",
    "server:build": "npm --prefix server run build",
    "server:start": "npm --prefix server run start",
    "client:start": "npm --prefix client start",
    "client:build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
